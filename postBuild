
INSTLOCATION=/home/jovyan/.local

# wget http://ceres-solver.org/ceres-solver-1.14.0.tar.gz
# tar zxf ceres-solver-1.14.0.tar.gz
# cd ceres-solver-1.14.0        
# mkdir build
# cd build     
# cmake ../ -DCMAKE_INSTALL_PREFIX=${INSTLOCATION}
# make -j3
# make install
# cd ../..

cd min_dtn
./setup.py install --user
                
# mkdir build
# cd build
# cmake ../ -DCMAKE_INSTALL_PREFIX=${INSTLOCATION}
# make
        
# ls -al
        
# export PYTHONPATH=$PYTHONPATH:/home/jovyan/min_dtn/build

# make install        
       


        
export BASEDIR=/home/jovyan/ngsuite
mkdir -p $BASEDIR
cd $BASEDIR
git clone https://github.com/NGSolve/ngsolve.git ngsolve-src
cd $BASEDIR/ngsolve-src
git submodule update --init --recursive
mkdir $BASEDIR/ngsolve-build
mkdir $BASEDIR/ngsolve-install
cd $BASEDIR/ngsolve-build
cmake -DCMAKE_INSTALL_PREFIX=${BASEDIR}/ngsolve-install ${BASEDIR}/ngsolve-src -DUSE_GUI=OFF
make
make install
export NETGENDIR="${BASEDIR}/ngsolve-install/bin"
export PATH=$NETGENDIR:$PATH
export PYTHONPATH=$NETGENDIR/../`python3 -c "from distutils.sysconfig import get_python_lib; print(get_python_lib(1,0,''))"`
